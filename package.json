{
  "name": "talks-api-client",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "node apps/talks-api/dist/main"
  },
  "devDependencies": {
    "turbo": "^1.9.3"
  },
  "engines": {
    "node": ">=18 <20"
  }
}
